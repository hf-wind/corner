FROM node:22-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN --mount=type=cache,target=/root/.npm \
    npm ci
COPY . .
ARG VITE_API_BASE=/api
ENV VITE_API_BASE=$VITE_API_BASE
ARG VITE_TURNSTILE_SITE_KEY
ENV VITE_TURNSTILE_SITE_KEY=$VITE_TURNSTILE_SITE_KEY
ARG VITE_TURNSTILE_ENABLED=true
ENV VITE_TURNSTILE_ENABLED=$VITE_TURNSTILE_ENABLED
ARG VITE_AMAP_WEB_KEY
ENV VITE_AMAP_WEB_KEY=$VITE_AMAP_WEB_KEY
ARG VITE_AMAP_SECURITY_CODE
ENV VITE_AMAP_SECURITY_CODE=$VITE_AMAP_SECURITY_CODE
ARG VITE_FEATURE_ALBUMS_ENABLED=true
ENV VITE_FEATURE_ALBUMS_ENABLED=$VITE_FEATURE_ALBUMS_ENABLED
ARG VITE_FEATURE_MAP_ENABLED=true
ENV VITE_FEATURE_MAP_ENABLED=$VITE_FEATURE_MAP_ENABLED
ARG VITE_FEATURE_PLACES_ENABLED=true
ENV VITE_FEATURE_PLACES_ENABLED=$VITE_FEATURE_PLACES_ENABLED
ARG VITE_FEATURE_CONSTELLATION_ENABLED=true
ENV VITE_FEATURE_CONSTELLATION_ENABLED=$VITE_FEATURE_CONSTELLATION_ENABLED
ARG VITE_FEATURE_STORIES_ENABLED=false
ENV VITE_FEATURE_STORIES_ENABLED=$VITE_FEATURE_STORIES_ENABLED
ARG VITE_SUPABASE_URL
ENV VITE_SUPABASE_URL=$VITE_SUPABASE_URL
ARG VITE_SUPABASE_ANON_KEY
ENV VITE_SUPABASE_ANON_KEY=$VITE_SUPABASE_ANON_KEY
ARG VITE_PUBLIC_APP_ORIGIN=https://corner.ink
ENV VITE_PUBLIC_APP_ORIGIN=$VITE_PUBLIC_APP_ORIGIN
RUN --mount=type=cache,target=/root/.npm \
    npm run build

FROM nginx:1.27-alpine
COPY nginx.conf /etc/nginx/conf.d/default.conf
COPY --from=builder /app/dist /usr/share/nginx/html
EXPOSE 3000
