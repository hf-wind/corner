[Unit]
Description=Corner production backup
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
User=ubuntu
Group=ubuntu
WorkingDirectory=/srv/corner/app
ExecStart=/usr/bin/env bash /srv/corner/app/scripts/backup.sh
Nice=10

[Install]
WantedBy=multi-user.target
