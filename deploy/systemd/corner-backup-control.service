[Unit]
Description=Process Corner backup and restore requests
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
User=ubuntu
Group=ubuntu
WorkingDirectory=/srv/corner/app
ExecStart=/usr/bin/env bash /srv/corner/app/scripts/backup-control.sh
Nice=10

[Install]
WantedBy=multi-user.target
